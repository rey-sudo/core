---
apiVersion: v1
kind: Service
metadata:
  name: limiter-store
  labels:
    role: limiter-store
spec:
  ports:
    - name: db
      protocol: TCP
      port: 6379
      targetPort: 6379
  clusterIP: None
  selector:
    role: limiter-store   
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: limiter-store
spec:
  serviceName: "limiter-store"
  replicas: 1
  selector:
    matchLabels:
      role: limiter-store
  template:
    metadata:
      labels:
        role: limiter-store
    spec:
      containers:
        - name: limiter-store
          image: redis
          resources:
            limits:
              memory: "500Mi"
            requests:
              memory: "256Mi"    